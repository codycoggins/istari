# Allow commond command lines to avoid pausing for prompts
# Use "source" to invoke this script
export ANTHROPIC_ALLOWED_COMMANDS="python,python3,pip,pip3,uvicorn,pytest,ruff,mypy,alembic,node,npm,npx,tsc,docker,docker-compose,ls,cat,head,tail,grep,mkdir,cp,mv,touch,chmod,ssh,scp,curl,wget,vi,nano,echo,find,sed,awk,sort,uniq,tr,wc,ps,nohup,tar,gzip,gunzip,zip,unzip,date,sleep,xattr,ditto,which"
